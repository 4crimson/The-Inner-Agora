# Memory

Long-term exports from Paperclip sessions go here.

Use:

```bash
node scripts/agora.mjs export-memory ISSUE
```

Generated session files are ignored by git by default, because they may contain private user questions or research notes.
